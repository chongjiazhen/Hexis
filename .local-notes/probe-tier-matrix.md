# Model-tier probe matrix

Cell = clean replies / total. Failure classes flagged below the count.

| persona | 4b |
|---|---|

| metric | 4b |
|---|---|
| peak VRAM (MiB) | 15964 |

Failure classes: leak, echo, repl, generic.

Floor verdict: the floor model is the largest tier that is clean fleet-wide AND has peak VRAM <= ~5500 MiB.
